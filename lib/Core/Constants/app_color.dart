import 'package:flutter/material.dart';

class AppColors {
  // Primary Colors
  static const Color primaryColor = Color(0xFF2F1A19);

  // Background Colors
  static const Color backgroundColor = Color(0xFFF5F5F5);

  // Card & Container Colors
  static const Color accentBeige = Color(0xFFE9D6AA);

  // Button Colors
  static const Color secondaryGold = Color(0xFF735A12);

  // Text Colors
  static const Color textPrimary = Color(0xFF2F1A19);
  static const Color textSecondary = Color(0xFF767373);

  // Addtional colors
  static const Color white = Color(0xFFFFFFFF);
  static const Color black = Color(0xFF000000);
}
